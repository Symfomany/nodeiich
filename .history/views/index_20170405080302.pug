extends layout.pug

block content
    h1 Liste des pages
    p= `Nombre de pages: ${pages.length} pages`
    ul
    each item in pages
        li #{item.clef}  
            a(href=`/edit/${page.id}`)
             i.fa.fa-pencil(aria-hidden='true')
             |   #{item.valeur}